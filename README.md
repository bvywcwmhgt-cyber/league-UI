# League UI (Static)

タブレット向けのリーグUI（順位表 / 日程 / 結果 / クラブ一覧）。
**GitHub Pages にアップロードするだけ**で動きます（サーバ不要）。

## 使い方
1. `index.html`, `app.js`, `styles.css` を同じ階層に置く
2. GitHub Pages を `main / (root)` に設定
3. 公開URLを開く

## データ保存
- ブラウザの `localStorage` に保存されます。
- 同じ端末・同じブラウザならリロードしても消えません。
